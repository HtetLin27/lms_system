// import { buildCourseScope } from "../utils/courseScope";

// const listCourses = async (req, res, next) => {
//     const scopeWhere = buildCourseScope(req.user);
//     const courses = await Course.findAll({ where: scopeWhere });
// }